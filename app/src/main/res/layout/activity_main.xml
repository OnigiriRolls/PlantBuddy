<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/activity_padding"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tCharacter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/tHelp"
        android:layout_centerInParent="true"
        android:layout_marginBottom="150dp"
        android:gravity="center"
        android:textSize="17sp"
        android:text="@string/hello_human_n_n_i_am_lily_your_plant_buddy"
        android:textFontWeight="500"
        app:drawableLeftCompat="@drawable/character_happy_small" />

    <TextView
        android:id="@+id/tHelp"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_above="@id/bScan"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp"
        android:gravity="center"
        android:textSize="17sp"
        android:text="@string/take_a_photo_help_identify_"
        android:textFontWeight="500" />

    <Button
        android:id="@+id/bScan"
        android:layout_width="150dp"
        android:layout_height="65dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp"
        android:onClick="onClick"
        app:icon="@drawable/ic_photo"
        app:iconGravity="textStart"
        app:iconPadding="1dp"
        app:iconSize="50dp" />

</RelativeLayout>